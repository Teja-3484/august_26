class Solution:
    def smallestNumber(self, n: int, t: int) -> int:

        while True:

            temp = n
            ans = 1

            while temp > 0:
                digit = temp % 10
                ans *= digit
                temp //= 10

            if ans % t == 0:
                return n

            n += 1
