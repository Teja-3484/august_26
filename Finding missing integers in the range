class Solution:
    def findMissingElements(self, nums: List[int]) -> List[int]:
        nums.sort()
        ans = []

        for i in range(len(nums) - 1):
            k = nums[i] + 1

            while k < nums[i + 1]:
                ans.append(k)
                k += 1

        return ans
